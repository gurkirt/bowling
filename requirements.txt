# Generated from environment.yml (conda) for pip installs
# Python version target: 3.11

numpy
pandas
pillow
opencv-python
scikit-learn
matplotlib
seaborn
altair
pytest
torch
torchvision
torchaudio
tensorboard
timm

# ffmpeg in environment.yml is a conda/system package, not a pip package.
# Install it separately on Linux if needed:
#   sudo apt-get install -y ffmpeg
